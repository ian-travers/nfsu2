<span
    class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full {{ $active ? 'bg-green-100 text-green-800' : 'bg-yellow-100 text-yellow-800'}}">
    {{ $active ? __('Active') : __('Trashed') }}
</span>
